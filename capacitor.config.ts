import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'custom-modal-close-button',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
